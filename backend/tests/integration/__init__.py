"""
Integration tests for the FastAPI application.

Validates that API routes, database models, and services work together
correctly. Uses TestClient with a test database session to exercise full
request/response cycles for every endpoint.
"""
