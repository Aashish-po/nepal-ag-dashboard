# Backend development dependencies — pinned to installed versions
# Install with: pip install -r requirements-dev.txt

# Linting & formatting
ruff==0.16.6

# Type checking
mypy==2.3.1

# Security scanning
bandit==1.9.4
pip-audit>=2.7.0

# Testing
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1

# Type stubs for third-party libraries
pandas-stubs==3.0.5.260730

# Build tooling — keep above pip-audit-flagged CVEs (PYSEC-2026-3447)
setuptools>=83.0.0
